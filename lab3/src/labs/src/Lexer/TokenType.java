package Lexer;
public enum TokenType {
    INTEGER,
    IDENTIFIER,
    IF,
    ELSE,
    WHILE,
    FOR,
    DO,
    INT,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    LEFT_PAREN,
    RIGHT_PAREN,
    EQUALS,
    GREATER_THAN,
    GREATER_THAN_OR_EQUAL,
    LESS_THAN,
    LESS_THAN_OR_EQUAL,
    NOT_EQUALS,
    ASSIGNMENT,
    LEFT_BRACE,
    RIGHT_BRACE,
    SEMICOLON,
    COMMA,
    AND,
    OR,
    NOT,
    NEWLINE,
    END_OF_FILE
}
